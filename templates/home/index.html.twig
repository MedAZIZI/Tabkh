{% extends 'base.html.twig' %}

{% block title %}
	MiaaaaM!
{% endblock %}

{% block body %}
	<style></style>
	<div class="container mx-auto w-full">
		<div class="gap-8 columns-16">
			<div class="text-start">
				<h1 class="text-6xl font-bold mt-8 pb-2" style="color:#5C9896;">Des recettes et des actus culinaires ✨
				</h1>

				<h2 class="text-2xl text-gray-600 mt-4 pb-2">Plus de 10 000 recettes faciles et rapides pour vous inspirer en cuisine. 🥐 🍩 🍲 🥗</h2>
			</div>

			<div class="divider divider-Neutral "></div>

			{# contenaire #}
			
				<div class="flex flex-wrap gap-12 justify-center align-start"> {% for recette in recettes %}
					<div class="card w-80 bg-base-100 shadow-xl min-w-50  ">
						<figure><img src="/assets/rec/{{recette.imageSrc}}" alt="food"/></figure>
						<div class="card-body">
							<h2 class="card-title">
								{{ recette.title }}
								{# <div class="badge badge-secondary">NEW</div> #}
							</h2>
							<p>{{ recette.description }}</p>
							<div class="card-actions justify-start py-1">
								
								<div class="badge badge-outline pb-0.5">Abricot</div>
								<div class="badge badge-outline ">Cannelle</div>

							</div>
							<div class="flex justify-end" style="color:#A7DCCD;">
								{# {{ dump(recette.id) }} #}
								<a href="{{ path('app_rec', {'id': recette.id}) }}">Voir plus</a>

							</div>


						</div>
					</div>
				{% endfor %}
			</div>
		</div>

		<div class="divider divider-Neutral "></div>

		


	{% endblock %}
